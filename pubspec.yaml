name: gemini_ai_chat
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  dash_chat_2: ^0.0.21
  firebase_core: ^3.4.0
  flutter:
    sdk: flutter
  flutter_gemini: ^2.0.5
  image_picker: ^1.1.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
